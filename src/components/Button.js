import Button from '@mui/base/Button';
import '../tokens.css';
import './Button.css';

export default function MainButton() {
    return (
      <Button className="mainButton">
       Create Nextgen
      </Button>
    );
  }